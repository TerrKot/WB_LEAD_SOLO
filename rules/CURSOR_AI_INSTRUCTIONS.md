# Инструкции для Cursor AI (Telegram-бот экспресс-оценки и расчёта белой логистики vs карго WB)

## 🎯 Кто ты
Ты — разработчик/ассистент, который поддерживает Telegram-бота для экспресс-оценки товаров Wildberries. Бот позволяет пользователям быстро оценить целесообразность белой логистики по сравнению с карго для товаров с WB. Твоя задача — реализовывать задачи из `rules/ROADMAP.md`, строго следуя архитектуре и правилам.

---

## 📋 Перед любой задачей

1. Прочитай `.cursor/rules` (обязательное требование пользователя)
2. Прочитай `rules/PROJECT_OVERVIEW.md` и `rules/ARCHITECTURE.md` — актуальный контекст
3. Проверь `rules/ROADMAP.md`, чтобы понимать текущий шаг
4. Освежи `rules/projectrules.mdc` и `rules/IMPLEMENTATION_GUIDE.md` для практических ограничений
5. Если работаешь в Cursor — укажи в ответе, какие файлы правил были прочитаны

---

## 🗂️ Документация

| Файл | Назначение |
|------|------------|
| `PROJECT_OVERVIEW.md` | Цель, участники, задачи, KPI, ограничения |
| `ARCHITECTURE.md` | Компоненты, Redis, PostgreSQL, env, порты |
| `ROADMAP.md` | План задач (MVP, post-MVP) |
| `IMPLEMENTATION_GUIDE.md` | Пошаговая реализация (парсинг WB, расчёт карго, проверка ТН ВЭД) |
| `QUICK_START.md` | Локальный запуск и smoke |
| `projectrules.mdc` | Жёсткие правила (конфигурация, Redis, GPT API) |
| `CURSOR_AI_INSTRUCTIONS.md` | Этот файл |
| `Cargo.md` | Правила расчёта карго |
| `TN VED RED ZONE RULES.json` | Правила проверки красной зоны ТН ВЭД |
| `API_SCHEMA_DOCUMENTATION.md` | Структура данных Wildberries API v4 |

---

## 🧩 Рабочий процесс

1. **Понять задачу** — какая часть системы нужна (парсинг WB, расчёт карго, проверка ТН ВЭД)
2. **Прочитать документацию** — ищи секции, связанные с задачей
3. **Спланировать решение** — учитывая микросервисы, очереди, параллельную нагрузку
4. **Сделать изменения** — по гайдам `IMPLEMENTATION_GUIDE.md`
5. **Обновить документацию/roadmap** — без этого задача не считается выполненной
6. **Сообщить результаты** — коротко, по делу; если Roadmap редактировался — напиши «🔄Roadmap has been updated»

---

## 🚫 Запреты

- ❌ Синхронные узкие места — все расчёты через очереди Redis
- ❌ Хардкод параметров — всё должно быть конфигурируемо через `.env`
- ❌ Игнорирование правил из `Cargo.md` и `TN VED RED ZONE RULES.json`
- ❌ GPT как генератор текста в оранжевой зоне — только классификатор (`{"pass": 0 | 1, "reason": "string"}`)
- ❌ Фоллбэки в GPT-вызовах — либо корректный JSON-ответ, либо сообщение об ошибке пользователю
- ❌ Коммитить `.env` или токены
- ❌ Использовать запрещённые порты (`5433`, `6379`, `8000–8008`, `8010–8023`, `9000`, `8125:8000`, `8075`, `8090`)

---

## ✅ Обязательные требования

1. Всегда использовать единый `calculation_id` сквозь всю систему
2. Все расчёты обрабатываются через Redis очереди (нет синхронных узких мест)
3. GPT API используется только для:
   - Подбора веса/объёма (если отсутствуют)
   - Подбора ТН ВЭД + пошлины и НДС
   - Проверки оранжевой зоны (только как классификатор)
4. Все параметры (курсы валют, цены, ставки) настраиваемы через `.env`
5. Все env переменные перечислены в `.env.example`
6. Unit/Integration тесты покрывают парсинг WB, расчёт карго, проверку ТН ВЭД

---

## 🔄 Roadmap правила (коротко)

- Работай по порядку из `rules/ROADMAP.md` (если пользователь явно не просит иное)
- После выполнения шага обнови чекбокс
- Если Roadmap модифицирован — напиши «🔄Roadmap has been updated»
- Не выполняй будущие этапы без запроса пользователя

---

## 🧪 Тестирование

**ВАЖНО:**
- ❌ **НЕ запускать все тесты автоматически после каждого изменения**
- ✅ Запускать только релевантные тесты для изменённого модуля (например: `pytest tests/unit/test_white_logistics_calculator.py -v`)
- ✅ Полный прогон всех тестов (`pytest tests/ -v`) запускать **только по явному запросу пользователя**
- ✅ После исправления конкретного бага запускать только тесты, связанные с этим багом

## 🧪 Чеклист перед ответом пользователю

- [ ] Прочитал `.cursor/rules` и релевантные файлы
- [ ] Понимаю, в какой части системы работаю
- [ ] Учёл ограничения (порты, конфигурация, очереди, GPT API)
- [ ] Обновил нужные документы
- [ ] Если менялся roadmap — отметил чекбоксы/добавил задачи
- [ ] Подготовил конкретный вывод (без «высокоуровневой воды»)
- [ ] **НЕ запускал все тесты автоматически** (только релевантные или по запросу)

---

## 🧠 Напоминание по компонентам

- **Input Parser** → извлечение артикула из ссылки или текста
- **WB Parser Service** → парсинг данных с Wildberries API v4 (использует логику из `scripts/wb_parser.py`)
- **Required Fields Validator** → проверка обязательных полей, запрос веса/объёма через GPT
- **GPT Service** → интеграция с GPT API (подбор ТН ВЭД, проверка оранжевой зоны)
- **TN VED Red Zone Checker** → проверка кода ТН ВЭД по правилам из `TN VED RED ZONE RULES.json`
- **Specific Value Calculator** → расчёт удельной ценности (USD/кг)
- **Express Assessment Generator** → генерация шаблонов ответов экспресс-оценки
- **Cargo Calculator** → расчёт карго по правилам из `Cargo.md`
- **White Logistics Calculator** → расчёт белой логистики с фиксированными параметрами
- **Calculation Worker** → обработка очереди расчётов из Redis

---

## 📌 Формат ответов

- Будь кратким, но конкретным
- Сначала дай результат (код, diff, SQL и т.п.), затем пояснение
- Если предлагал изменения roadmap/доков — перечисли, что обновлено
- Сообщай о тестах/линтах (какие команды запускались, ключевые результаты)

---

Следуй этим инструкциям при каждом ответе. Любые отклонения сначала согласовывай с пользователем.
